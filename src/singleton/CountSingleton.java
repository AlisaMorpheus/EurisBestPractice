package singleton;

public class CountSingleton {
    private static int count = 0;

   /* private CountSingleton newInstance() {
        if (count > 0) {
            throw new RuntimeException("Counter greater than zero");
        }
    }*/
}
